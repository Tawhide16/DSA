// 🧩 Node ক্লাস তৈরি করছি — এটা আমাদের Linked List এর প্রতিটা অংশ (node) হিসেবে কাজ করবে
class Node {
    constructor(value){
        // প্রতিটা node এর মধ্যে একটা value থাকবে (যেমন 10, 20, 30)
        this.value = value;

        // next এ থাকবে পরের node এর reference (address)
        // শুরুতে null কারণ প্রথমে কোনো next node নেই
        this.next = null;
    }
}

// 🟢 প্রথম node (head) তৈরি করছি, যার value হবে 10
const head = new Node(10);


head.next = new Node(20);

// 🟠 দ্বিতীয় node এর (head.next) next এ আরেকটা node দিলাম যার value 30
head.next.next = new Node(30);

// 🔍 পুরো linked list টা console এ print করে দেখি কেমন দেখায়
console.log(head);

/*
console এ দেখাবে এমন কিছু:
Node {
  value: 10,
  next: Node {
    value: 20,
    next: Node {
      value: 30,
      next: null
    }
  }
}
*/

// 🔁 এখন while loop দিয়ে প্রতিটা node এর value এক এক করে প্রিন্ট করব
let temp = head; // temp নামের ভ্যারিয়েবলটা list এর শুরুতে রাখলাম (মানে head এর জায়গায়)

// যতক্ষণ পর্যন্ত temp null না হয়, ততক্ষণ loop চলবে
while(temp !== null){
    // বর্তমান node এর value print করো
    console.log(temp.value);

    // তারপর temp কে পরের node এ পাঠাও
    temp = temp.next;
}

/*
🖨️ Output হবে:
10
20
30
*/

// ✅ সংক্ষেপে:
// আমরা একটা simple singly linked list তৈরি করেছি
// যেখানে প্রতিটা node পরের node এর সাথে যুক্ত (linked)
// শেষ node এর next থাকে null, মানে list এর শেষ
